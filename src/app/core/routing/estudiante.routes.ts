import { Routes } from '@angular/router';

/**
 * Rutas exclusivas del rol **Estudiante** (p. ej. `/estudiante/...`).
 * Añade aquí rutas y guard cuando definas el área del estudiante.
 */
export const ESTUDIANTE_ROUTES: Routes = [];
