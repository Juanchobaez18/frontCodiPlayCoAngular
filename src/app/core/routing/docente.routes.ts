import { Routes } from '@angular/router';

/**
 * Rutas exclusivas del rol **Docente** (p. ej. `/docente/...`).
 * Añade aquí `path`, `loadComponent` y `canActivate: [docenteGuard]` cuando definas el flujo.
 */
export const DOCENTE_ROUTES: Routes = [];
